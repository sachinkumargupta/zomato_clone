<div class="well col-sm-8 col-sm-offset-2">
  <h3>Order Details</h3>
  <table class="table table-stripted table-responsive">
    <tr>
      <th>Food</th>
      <th>Quantity</th>
      <th>Price</th>
      <th>Total</th>
    </tr>
    <% @order_array.each do |order| %>
      <tr>
        <td><%= order[0] %></td>
        <td><%= order[1] %></td>
        <td><%= order[2] %></td>
        <td><%= order[1] * order[2] %></td>
      </tr>
    <% end %>
  </table>

  <div class="additional-order-details">
    <p class="grand-total"><strong>Grand Total:</strong> <%= @order.total %> rupees</p>
    <p><strong>Deliver To:</strong> <%= @order.user.name %></p>
    <p><strong>Address:</strong> <%= @order.delivery_address %></p>
    <p><strong>Order Placed at: </strong><%= @order.created_at.strftime("%I:%M %p %d-%m-%Y") %> UTC</p>
  </div>
  <div class="btn-group">
    <%= link_to 'Back', restaurant_orders_path(@restaurant), class: "btn btn-default" %> 
  </div>
</div>
